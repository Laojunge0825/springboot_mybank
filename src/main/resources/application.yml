server:
  port: 80
  session.timeout: 60000
  servlet.contextpath: /mybank
logging:
  level:
    root: warn
    com.yc: info
    org.springframework: warn
  file:
    path: ./logs
    name: yc.myback

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/TestBank?serverTimezone=GMT%2B8
    username: root
    password: a
swagger:
  enabled: true